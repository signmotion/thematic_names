name: names
description: A starting point for Dart libraries or applications.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  dart_helpers:
    path: ../dart_helpers

dev_dependencies:
  json_annotation: ^4.8.1
  lints: ^3.0.0
  test: ^1.25.1
